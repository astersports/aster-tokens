/**
 * @aster/tokens — canonical Aster design system values (v0.3.1).
 * Values-only: no components, no runtime dependencies.
 *
 *   import { tokens, typography } from "@aster/tokens";
 *   import surfaceClasses from "@aster/tokens/surface-classes.json" with { type: "json" };
 *
 * CSS mirrors: "@aster/tokens/tokens.css" (colors) + "@aster/tokens/typography.css" (type).
 */
export { tokens } from "./tokens.js";
export { typography } from "./typography.js";
